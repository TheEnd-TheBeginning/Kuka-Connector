# Kuka-Connector

This app allows users to interact with KUKA robots through the C3 Bridge Interface Server.\
The app uses sockets to discover robots on the network and connect to them.\
Users can read the robot's current coordinates and modify them.
